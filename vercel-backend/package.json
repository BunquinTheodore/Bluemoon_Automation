{
  "name": "bluemoon-backend",
  "version": "1.0.0",
  "description": "Serverless backend for Bluemoon Automation - Photo cleanup and email reports",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "typecheck": "tsc --noEmit --strict --esModuleInterop --skipLibCheck --target es2020 --module commonjs --moduleResolution node api/*.ts lib/*.ts"
  },
  "dependencies": {
    "@sendgrid/mail": "^8.1.0",
    "firebase-admin": "^12.0.0"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
